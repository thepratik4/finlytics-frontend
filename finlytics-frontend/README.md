# Finlytics Frontend

This folder contains a modern React + Vite frontend for the Finlytics backend.

Quick start (in PowerShell):

```powershell
cd finlytics-frontend
npm install
npm run dev
```

By default Vite proxies `/api` and `/analysis` to `http://localhost:5000` (adjust `vite.config.js` if needed).
